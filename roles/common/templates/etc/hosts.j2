127.0.0.1 localhost

{% for host in groups.all %}
{{ hostvars[host]['ansible_' + iface]['ipv4']['address'] }} {{ hostvars[host]['ansible_hostname'] }} {{ hostvars[host]['ansible_hostname'] }}.{{ domain }}
{% endfor %}
